<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.MinimalTimer" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorOnPrimary">@color/background_dark</item>
        <item name="colorOnSecondary">@color/background_dark</item>
        <item name="colorOnBackground">@color/text_primary</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:navigationBarColor">@color/background_dark</item>
    </style>

    <!-- Timer Display Style -->
    <style name="TimerDisplayStyle">
        <item name="android:textSize">60sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">sans-serif-light</item>
        <item name="android:letterSpacing">0.1</item>
    </style>

    <!-- Input Style -->
    <style name="InputStyle" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/primary</item>
        <item name="hintTextColor">@color/text_secondary</item>
    </style>

    <!-- Button Style -->
    <style name="PrimaryButtonStyle" parent="Widget.Material3.Button.Icon">
        <item name="android:padding">12dp</item>
        <item name="iconPadding">0dp</item>
        <item name="iconGravity">textStart</item>
        <item name="cornerRadius">12dp</item>
    </style>

    <style name="SecondaryButtonStyle" parent="Widget.Material3.Button.OutlinedButton.Icon">
        <item name="android:padding">12dp</item>
        <item name="iconPadding">0dp</item>
        <item name="iconGravity">textStart</item>
        <item name="cornerRadius">12dp</item>
        <item name="strokeColor">@color/text_secondary</item>
        <item name="iconTint">@color/text_secondary</item>
    </style>
</resources>
